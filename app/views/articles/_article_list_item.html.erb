<a href="<%= article_path(article) %>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center" style="width: 100%">
    <div class="ms-2 me-auto align-self-start">
        <div class="fw-bold"><%= article.title %></div>
        <%= article.body %>
    </div>
    <!--
        <a href="<%= article_path(article) %>" class="btn btn-primary">&gt</a> <-- it's equal to link to below
    <%= link_to ">", article, class: 'btn btn-primary align-self-end m-1' %>
    -->
</a>
